import React from "react";
import "./App.css";

import Viewport from "./components/Viewport";

const App: React.FC = () => {
  return (
    <div className="App">
      <Viewport />
    </div>
  );
};

export default App;
